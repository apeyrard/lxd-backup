sudo: required
dist: trusty
language: python
python:
  - 3.5
before_install:
  - sudo apt-get -qq update
  - sudo apt-get -t trusty-backports -y install lxd
  - sudo lxd init --auto --storage-backend=dir || true
  - pip install tox-travis
script:
  - sudo tox
